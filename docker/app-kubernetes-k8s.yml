apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: vnptcloud
  name: app-kubernetes-deployment
  labels:
    app: app-kubernetes
spec:
  replicas: 1
  selector:
    matchLabels:
      service: app-kubernetes
  template:
    metadata:
      labels:
        app: app-kubernetes
        service: app-kubernetes
    spec:
      containers:
        - name: app-kubernetes
          image: registry.onsmartcloud.com/cloudportal/oneportal-app-smart-cloud
          ports:
            - containerPort: 80
              protocol: TCP
      imagePullSecrets:
        - name: regcred
#          env:
#            - name: API_LINK
#              value: http://localhost:30991
---
apiVersion: v1
kind: Service
metadata:
  name: app-kubernetes
  namespace: vnptcloud
spec:
  type: NodePort
  ports:
    - port: 80
      name: port1
      nodePort: 30766
  selector:
    app: app-kubernetes
